

function Greeting(props){
    return <h1> Hello, {props.name}!!!</h1>;
}

export default Greeting;






// Props are Read-Only
// Once passed to a component, props cannot be changed by that component
// Props are the ingredients you give the recipe.
// The component (recipe) uses those ingredients to make something tasty (the UI).


